#include <bits/stdc++.h>
using namespace std;

int main(){
    unordered_set<int> st;

    // lower_bound() and upper_bound() funcdoes not works in unordered set, rest all func are same
    // it does not store in any particular order
    //  it has better complexity than set in most cases, except some when collision happens which is rare
    // in most cases time complexity  is O(1) for all oprn, but in worst case O(n)

    return 0;
}